import { Box, Typography } from '@mui/material';

export function AllSharesHeader() {
  return (
    <Box sx={{ display: 'flex' }}>
      <Typography variant={'h4'}>My Shared Lists</Typography>
    </Box>
  );
}
